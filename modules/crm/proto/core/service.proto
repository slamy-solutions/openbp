syntax = "proto3";

package crm_core_service;

option go_package = "slamy/openbp/crm/core/service;service";

message PingRequest {}
message PingResponse {}

service ServiceService {
    // Responses asap if server is alive
    rpc Ping(PingRequest) returns (PingResponse) {};
}